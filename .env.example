# Cloudflare Worker URL for Contact Form
# After deploying the worker, set this to your worker URL
# Example: https://contact-form-worker.YOUR-SUBDOMAIN.workers.dev
# Or your custom route: https://alan.one/api/contact-worker
# 
# If not set, the contact form will use the default /api/contact endpoint
PUBLIC_CONTACT_WORKER_URL=
